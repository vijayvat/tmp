condition = !anytrue([for rule in self.security_rule : rule.access == "Allow" && rule.direction == "Inbound" && (rule.source_address_prefix == "*" || (try(rule.source_address_prefix, null) == null && length(try(rule.source_address_prefixes, [])) == 0 && length(try(rule.source_application_security_groups, [])) == 0))])
